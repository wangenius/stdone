import {Layout} from "../components/Layout";
import React from "react";

const Reward = () => {
    return <Layout>
        <img alt={'reward'} style={{width:300}} className={"mx-auto mt-20"} src={'/wechat_reward.png'}/>
    </Layout>
}

export default Reward